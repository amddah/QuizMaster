<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Optional size: ImageView already sets dimensions, so this can be omitted if preferred -->
    <size
        android:width="84dp"
        android:height="84dp" />

    <solid android:color="@color/white" />

    <!-- thin border using the project's primary green color if available -->
    <stroke
        android:width="1dp"
        android:color="@color/green_primary" />

</shape>